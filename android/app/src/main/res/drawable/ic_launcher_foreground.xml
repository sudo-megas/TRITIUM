<?xml version="1.0" encoding="utf-8"?>
<!-- Empty on purpose — the maker's own artwork (build/icons/) is the
     background layer in full; there is no separate foreground graphic to
     draw over it, the same one-image treatment F13.md gives the desktop
     icon. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108" />
